# FARO PROTOCOL — Dependencias Python
# Instalación: pip install -r requirements.txt

# ── Procesamiento satelital ────────────────────────────────────────
numpy>=1.24.0
rasterio>=1.3.0
scipy>=1.10.0
scikit-image>=0.20.0

# ── Visualización ─────────────────────────────────────────────────
matplotlib>=3.7.0
Pillow>=9.5.0

# ── HTTP / APIs ───────────────────────────────────────────────────
requests>=2.28.0

# ── Inteligencia artificial (MachinaOS) — costo $0 via Groq ──────
groq>=0.9.0

# ── Scheduling automático (faro_price_updater --daemon) ───────────
APScheduler>=3.10.0

# ── Variables de entorno (.env) ───────────────────────────────────
python-dotenv>=1.0.0

# ── Portal seguro — CAPA 7 (gen_portal_key.py con Firebase Admin) ─
firebase-admin>=6.5.0

# ── Manual de cliente — TAREA 2 (faro_manual_cliente.py, PDF) ─────
reportlab>=4.2.0
